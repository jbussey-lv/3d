function flatten(){

	// go through every point and find their flattened coords
	for(key in points){
		var point = points[key];
		point.flat = flatten_point(point);
	}

	var flatten_point = function(point){
		
		// calculate x and y, set them as anonymous object params, and return the object
	
	
	
	
	
	
	}


}